{
  "name": "tiny-erp-client",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "build": "npm run build:types && esbuild ./src --platform=node --format=cjs --bundle --minify --outfile=dist/index.js",
    "build:types": "tsc --emitDeclarationOnly",
    "start": "npm run build && node dist",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "Carlos Dibb",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@eslint/js": "^9.2.0",
    "esbuild": "^0.21.1",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.8.0"
  }
}
