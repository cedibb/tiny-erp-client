{
  "name": "tiny-erp-client",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "build": "npm run typecheck && esbuild ./src/client --platform=node --format=cjs --bundle --minify --outfile=dist/index.js --sourcemap",
    "start": "npm run build && node dist",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "author": "Carlos Dibb",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@eslint/js": "^9.2.0",
    "esbuild": "^0.21.1",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "typescript-eslint": "^7.8.0"
  }
}
